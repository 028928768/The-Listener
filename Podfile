# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'The Listener' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for The Listener

  target 'The ListenerTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'The ListenerUITests' do
    inherit! :search_paths
    # Pods for testing
  end
	pod 'Firebase/Core'
	pod 'Firebase/Auth'

	source 'https://github.com/CocoaPods/Specs.git'
    	pod 'Alamofire', '~> 4.7'
	
end
